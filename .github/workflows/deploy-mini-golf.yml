name: Deploy mini-golf-flow demo to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'mini-golf-flow/**'
      - 'src/**'
      - '.github/workflows/deploy-mini-golf.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npx vite build --config mini-golf-flow/vite.config.ts
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist-mini-golf

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
