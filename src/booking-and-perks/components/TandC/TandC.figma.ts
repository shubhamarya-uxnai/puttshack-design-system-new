// url=https://www.figma.com/design/X5YJsGIXBKazkrUaxk0jR9/Booking-and-Perks-Flow?node-id=4199-165050
// source=src/booking-and-perks/components/TandC/TandC.tsx
// component=TandC
import figma from 'figma'

// Figma captured no properties at all for this node (empty `props`) — there
// is nothing to read via getString/getBoolean/getEnum here. `children` is a
// React-only prop with no Figma-side source, so the component renders with
// its own placeholder legal copy.

export default {
  example: figma.code`
    <TandC />
  `,
  imports: ['import { TandC } from "./TandC"'],
  id: 'tandc',
  metadata: { nestable: true },
}
